<div>
  <h1>All rents</h1>
</div>

<div class="right"><%= link_to 'New list', new_list_path, class: "btn btn-flat" %></div>
<ul>
  <% @lists.each do |list| %>
    <li><%= list.name %> <%= link_to 'Show', list_path(list) %></li>
  <% end %>
</ul>