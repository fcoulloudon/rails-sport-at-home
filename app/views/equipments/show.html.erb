<div class="container" style="width: 80%;">
  <div class="row rounded shadow-sm border-2 mt-3 p-3">
    <div class="col-md-6 mb-4 mb-lg-0">
      <h2><%= @equipment.title %></h2>
      <p><%= @equipment.description %></p>
      <p>Daily rate: <%= @equipment.daily_rate %>€</p>
    </div>
    <div class="col-md-6 mb-4 mb-lg-0">
      <img src="<% if @equipment.image.key? %><%= cl_image_path @equipment.image.key, height: 200, crop: :pad
      %><% else 
      %>https://res.cloudinary.com/df8y5fexl/image/upload/v1620479501/placeholder_rvxg16.jpg<% 
      end %>" style="height: 200px;" alt="" class="img-fluid d-block mx-auto mb-3">
    </div>

      <%= link_to "Back to equipments", equipments_path %>
    <% if (current_user != nil && current_user.user_type === "User") %>
      &nbsp;&nbsp;-&nbsp;&nbsp;
      <%= link_to 'Rent',  new_equipment_rent_path(@rent, equipment_id: @equipment.id) %>
    <% elsif (current_user != nil && current_user.user_type === "Owner" && current_user.id === @equipment.user_id) %>
      &nbsp;&nbsp;-&nbsp;&nbsp;
      <%= link_to "Edit equipment", edit_user_equipment_path(@equipment) %>
    <% end %>
  </div>
</div>

